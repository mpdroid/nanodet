# Linux smoke / dev — align with gyolo/requirements-yenv.txt (Python 3.10, scientific pins).
#
# NanoDet requires torch>=1.10,<2.0, which is compatible with the gyolo yenv wheel.
# Install PyTorch FIRST (same as gyolo yenv):
#   pip install torch==1.13.0+cu116 torchvision==0.14.0+cu116 torchaudio==0.13.0+cu116 \
#     --extra-index-url https://download.pytorch.org/whl/cu116
#
# Then install nanodet in editable mode (required — nanodet is not on PyPI):
#   pip install -e backends/nanodet
#
# Then:
#   pip install -r backends/nanodet/requirements-linux-yenv.txt
#
# PYTHON=python3.10 ./scripts/setup_venvs.sh --linux-yenv nanodet

# --- Build / runtime bootstrap ---
setuptools==69.5.1        # pkg_resources broken in setuptools>=71 on some venv configs; 69.x is safe

# --- NanoDet-specific ---
Cython==0.29.33
imagesize==1.4.1
omegaconf==2.2.3
pyaml==21.10.1
pycocotools==2.0.6
pytorch-lightning==1.9.5
tabulate==0.9.0
termcolor==2.2.0
torchmetrics==0.11.4          # <0.12 required by pytorch-lightning 1.9.x

# --- Export ---
onnx==1.12.0
onnx-simplifier==0.4.8        # 0.4.1 has no pre-built wheel — git describe fails outside a repo
onnxruntime-gpu==1.12.0

# --- Mirrors gyolo/requirements-yenv.txt (no torch/torchvision — installed above) ---
matplotlib==3.6.2
numpy==1.23.4
opencv-python==4.6.0.66
Pillow==9.3.0
PyYAML==6.0
requests==2.28.1
scipy==1.9.3
tensorboard==2.13.0
tqdm==4.64.1
pandas==1.5.1
seaborn==0.12.1
psutil==5.9.4
